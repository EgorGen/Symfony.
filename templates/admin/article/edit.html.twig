{% extends 'base.html.twig' %}

{% block title %}Редактировать статью{% endblock %}

{% block body %}
    <h1>Редактировать статью</h1>

    {{ include('admin/article/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a class="btn btn-secondary" href="{{ path('admin') }}">{{ 'back_to_list'|trans }}</a>

    {{ include('admin/article/_delete_form.html.twig') }}
{% endblock %}
